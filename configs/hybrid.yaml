experiment_name: hybrid-rq1
seed: 1337
backend: hybrid
model:
  checkpoint_path: assets/cifar10-resnet20-0.pkl
data:
  root: assets/cifar-10-batches-py
  subset_size: 1024
  batch_size: 64
  cpu_batch_size: 4
  num_workers: 0
grid:
  resolution: 8
  scale: 1.0
runtime:
  num_batches: null
  output_root: outputs
  preload: false
resources:
  cpu_workers: 2
