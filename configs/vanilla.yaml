experiment_name: vanilla
seed: 1337
backend: vanilla
model:
  checkpoint_path: assets/cifar10-resnet20-0.pkl
data:
  root: assets/cifar-10-batches-py
  subset_size: 1024
  batch_size: 64
  num_workers: 0
grid:
  resolution: 16
runtime:
  num_batches: null
  output_root: outputs
  preload_gpu_batches: false
  compile_gpu_chunk_eval: false
resources:
  cpu_workers: 0
decomposition:
  gpu_initial_ratio: 1.0
  fixed_gpu_chunk_size: null
  gpu_chunk_size_max: 256
  cpu_chunk_size: 1
  cpu_threads_per_worker: 1
